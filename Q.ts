// String literal types
type CanvasApps = "Word" | "PowerPoint" | "Sway";

function getCanvasEmployees(app: CanvasApps)
{
    switch(app)
    {
        case "Word":
            break;
        case "Excel":
            break;
    }
}